namespace Chel.Abstractions.Parsing
{
    public class EndOfBlockToken : Token
    {
        public static EndOfBlockToken Instance = new EndOfBlockToken();

        public EndOfBlockToken()
        {
        }
    }
}
